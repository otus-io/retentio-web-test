# Loaded automatically by `npm run dev` (Vite development mode).
# To use a local backend instead, create `.env.development.local` with:
#   VITE_API_URL=http://localhost:8080
VITE_API_URL=https://api.retentio.app:8443

