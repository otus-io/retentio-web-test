---
description: WordUpX project-wide conventions for commits, PRs, docs, and code style
alwaysApply: true
---

# WordUpX Project Conventions

## Project Overview
WordUpX is a vocabulary learning app with a Go backend (gorilla/mux, Redis, JWT) and Flutter frontend (Riverpod, go_router, Dio).

## Commit & PR Conventions
- Commit/PR title: `<type>(<scope>): <subject>` in imperative mood, under 50 chars
- Types: feat, fix, docs, style, refactor, perf, test, chore
- Scopes: auth, deck, card, fact, media, api, frontend, ci
- Keep PRs under 200 lines, commits under 100 lines
- Squash and merge for feature branches

## Code Style
- **Go**: Follow Effective Go, run `gofmt`, lines ≤120 chars, functions ≤50 lines
- **Dart**: Follow Effective Dart, run `dart format`, lines ≤120 chars, functions ≤50 lines
- Prefer clear naming over comments; add comments only for complex logic

## Documentation
- All user-facing docs must have English and Chinese versions (e.g., `FOO.md` + `FOO_zh.md`)
- Keep `docs/API_PROGRESS_TRACKER.md` updated when adding/changing endpoints

## Key Principles
- Never modify production code to make tests pass — tests expose bugs for developers to fix
- RESTful API design (no RPC-style endpoints) — see `docs/design-doc/restful-api-design.md`
- CI must pass before merge: `gofmt`, `go vet`, `go test`, `dart format`, `flutter analyze`, `flutter test`
